"""Django app configuration for assignment templates."""

from django.apps import AppConfig


class AssignmentTemplatesConfig(AppConfig):
    """AppConfig for the assignment-template domain."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "assignment_templates"
