services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    env_file:
      - ./env/.env.production
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      app: {}

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./env/.env.production
    environment:
      MEDIA_ROOT: /srv/lattice-prod/media
    volumes:
      - media-data:/srv/lattice-prod/media
      - artifact-data:/srv/lattice-prod/media/artifacts
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_started
    networks:
      app: {}
      lattice-proxy:
        aliases:
          - lattice-prod-backend

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./env/.env.production
    expose:
      - "3000"
    depends_on:
      backend:
        condition: service_started
    networks:
      app: {}
      lattice-proxy:
        aliases:
          - lattice-prod-frontend

volumes:
  db-data:
    name: lattice-prod-db-data
  media-data:
    name: lattice-prod-media-data
  artifact-data:
    name: lattice-prod-artifact-data

networks:
  app:
    name: lattice-prod-app
  lattice-proxy:
    external: true
    name: lattice-proxy
