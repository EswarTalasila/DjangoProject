services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    env_file:
      - ./env/.env.testing
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      app: {}

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./env/.env.testing
    environment:
      MEDIA_ROOT: /srv/lattice-test/media
    volumes:
      - media-data:/srv/lattice-test/media
      - artifact-data:/srv/lattice-test/media/artifacts
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_started
    networks:
      app: {}
      lattice-proxy:
        aliases:
          - lattice-test-backend

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    restart: unless-stopped
    env_file:
      - ./env/.env.testing
    expose:
      - "3000"
    depends_on:
      backend:
        condition: service_started
    networks:
      app: {}
      lattice-proxy:
        aliases:
          - lattice-test-frontend

volumes:
  db-data:
    name: lattice-test-db-data
  media-data:
    name: lattice-test-media-data
  artifact-data:
    name: lattice-test-artifact-data

networks:
  app:
    name: lattice-test-app
  lattice-proxy:
    external: true
    name: lattice-proxy
